---
title: "Docker → Kubernetes (K3s) in a Multi-Tenant Environment"
description: "A practical checklist from the K3s migration: rollout, per-tenant isolation, and the trade-offs that kept incidents at bay."
pubDate: "2026-02-20"
category: "DevOps"
lead: "Migrating to K3s in a multi-tenant setup demands rollout and observability discipline from the very first deploy."
tags:
  - "Kubernetes"
  - "K3s"
  - "multi-tenant"
  - "migration"
  - "SRE"
---

import Callout from "../../../components/blog/Callout.astro";
import InteractiveChecklist from "../../../components/blog/InteractiveChecklist.astro";
import DecisionCard from "../../../components/blog/DecisionCard.astro";
import TradeoffCompare from "../../../components/blog/TradeoffCompare.astro";

## Context

The operation grew to serve multiple clients, and the Docker Compose-based model started causing friction in deployment, scalability, and troubleshooting.

The goal was to migrate to K3s with minimal downtime, while preserving per-tenant governance and cost predictability.

## Action

<InteractiveChecklist
  title="Migration rollout"
  items={[
    "Map network, storage, and secrets dependencies before the migration.",
    "Separate workloads by namespace with per-tenant resource limits, using a standardized Helm values pattern per tenant.",
    "Start with a pilot tenant and validate runbook, metrics, and rollback before scaling to the remaining client groups.",
    "Standardize probes, requests/limits, and operational labels across all services.",
    "Instrument latency, error, and saturation metrics, with automated smoke tests after every rollout.",
    "Formalize rollback playbooks for every critical service, with automatic post-deploy validation.",
  ]}
/>

<Callout type="tip" title="Start with a pilot tenant">
  Validate the runbook, metrics, and rollback before scaling.
</Callout>

<DecisionCard
  title="Rollout order"
  decision="Migrate one pilot tenant before the remaining client groups."
  rationale="Validate the runbook, metrics, and rollback within a limited area."
  consequence="Configuration problems surface before they can affect the entire base."
/>

## Result

Migration completed with no availability incident noticeable to tenants. The phased rollout caught configuration issues in the pilot tenant before they could affect the entire base, and the smoke-test checklist caught regressions before they turned into incidents.

<TradeoffCompare
  title="Phased or full rollout"
  options={[
    { title: "Pilot tenant", description: "Limits validation scope and exposes configuration problems early." },
    { title: "All tenants", description: "Takes problems to full scale and increases rollback cost." },
  ]}
/>

## Lessons learned

- Migrating all tenants at once, without a pilot phase, would have hidden problems until they hit full scale — where the cost of rollback would have been much higher.

- Ignoring per-tenant resource limits creates a noisy-neighbor problem: one noisy tenant drags down the performance of others on the same node.

- An informal rollback process for a critical service is technical debt disguised as time savings — it comes due the first time a migration goes wrong.
